"""Runnable experiments: model comparison, hyperparameter searches, and the
diagnostic sweeps behind the numbers in WRITEUP.md.

Each module exposes a `main()` and runs standalone:

    python -m rcpm.experiments.compare_models
"""
